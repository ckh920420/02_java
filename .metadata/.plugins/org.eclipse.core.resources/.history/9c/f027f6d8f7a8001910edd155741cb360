package marker;
/**
 * 보드마카를 정의하는 클래스
 * --------------------------
 * 보드마카의 속성정의
 * 색상 : color : String
 * 잉크잔량 : isNew : boolean
 * 마개의 개폐여부 : isOpened : boolean
 * ----------------------------
 * 생성자 정의 : 
 * 매개변수를 받는 생성자 : 모든 필드에 적용
 * ---------------------------------
 * 기능정의 : 메소드로 생성
 * 잉크가 꽉차있을 떄 : use() : void "새거" or "헌거"
 * 마개 개폐에 따라 : print() : void " 열림" or " 닫힘"
 *                               
 * 
 * @author Administrator
 *
 */

public class Marker {
	
	//1. 맴버 변수 선언
	String color;
	boolean isNew;
	boolean isOpened;
	
	//2. 생성자 선언
	Marker ( String color, boolean isNew, boolean isOpened ) {
		this.color = color;
		this.isNew = isNew;
		this.isOpened = isOpened;
	}
	//3. 메소드 선언
	public void use() {
		System.out.printf("%s색의 마커를 사용하고 있습니다.%n,color");
		
	}

	public void print() {
		String message = "마커 색상 : %s, %s, %s ";
		String openStatus;
		String newStatus;
		
		if (isOpened) {
			openStatus = "뚜껑이 열려 있습니다.";
			} else {
				open
			}
		
		
	}
	
	//(3) 출력 메소드 print()
	public void print() {
		System.out.printf(" 귀하의 보-드마카는 현재 %s 이며, %");
	}
}
