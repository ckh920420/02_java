package hello;

public class MethodDeclare {
	//1. 변수선언부
	String input;
	char[] letters;

	//2. 생성자 선언부
	MethodDeclare(){
	}
	//3. 메소드 선언부
	public return convertStringToCharArray {
	for (idx = 0; idx == input.length(); idx=+1)
	
	System.out.printf("%s",idx);
	}
	}

/**
 * 여러분들이 책을 사러 갔어요
 * 서점에 내가 찾는 책이 어디있는지 몰라요
 * 일단 교보문고에 가서 내가 찾는 책이 있는 코너까지는 갔는데 책을 못찾아요
 * 직원한테 물어봤어요
 * 직원은 물론 시스템검색을 하긴하지만 
 * 여러분에게 책 찾아줄게요 하고 어딘가에 갔어요
 * 그 직원은 위에서 세번쨰 칸에 왼쪽에서 다섯번쨰 책을 여러분께 가져다 줬어요
 * 
 * 그러면 여러분이 한권의 책을 사기위해서 서점이 필요하고 직원이 필요하고 
 * 
 * 책 한권에 대한 정의
 * 책장에 대한 정의
 * 책장을 관리하는 북매니저에 대한 정의
 * 책이 있는 서점이 있는 정의
 * 여러분이 책을 찾을 때 총 4가지의 클래스가 필요합니다
 * 
 * 자 이제부터 프로젝트를 켭시다
